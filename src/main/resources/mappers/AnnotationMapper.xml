<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.markuppdf.mapper.AnnotationMapper">

  <select id="findAnnotationsByDocId" parameterType="string" resultType="com.example.markuppdf.dto.AnnotationDto">
    SELECT
      DOC_ID AS docId,
      PAGE_NUM AS pageNum,
      ANNOTATION_DATA AS annotationData
    FROM
      ANNOTATIONS
    WHERE
      DOC_ID = #{docId}
  </select>

  <insert id="saveAnnotation" parameterType="com.example.markuppdf.dto.AnnotationDto">
    MERGE INTO ANNOTATIONS KEY(DOC_ID, PAGE_NUM)
      VALUES (#{docId}, #{pageNum}, #{annotationData})
  </insert>

</mapper>