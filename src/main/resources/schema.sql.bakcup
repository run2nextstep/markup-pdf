-- H2 데이터베이스는 기존 테이블이 있어도 오류 없이 실행되도록 IF NOT EXISTS 옵션을 추가할 수 있습니다.
CREATE TABLE IF NOT EXISTS ANNOTATIONS (
  DOC_ID VARCHAR(255) NOT NULL,
  PAGE_NUM INT NOT NULL,
  ANNOTATION_DATA CLOB,
  PRIMARY KEY (DOC_ID, PAGE_NUM)
  );